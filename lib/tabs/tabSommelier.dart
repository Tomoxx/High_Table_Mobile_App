import 'package:flutter/material.dart';

class TabSommelier extends StatelessWidget {
  const TabSommelier({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Sommelier'),
    );
  }
}